version: 1

web:
  runtime: worker

server:
  cwd: './'
  script: './bootstrap.sh'

routes:
  - src: /api/**
    dest:
      type: server

  - src: /**
    dest:
      type: web
      path: /index.html
